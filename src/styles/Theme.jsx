import { createTheme } from "@mui/material";


const theme = createTheme({
    palette: {
      primary: {
        main: "#022C43",
      },
      backgroundColor: {
        default: "#848484"
      }
    }
  });

  export default theme;